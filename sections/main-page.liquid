<div class="page-content container">
  <h1 class="page-content__title">{{ page.title }}</h1>
  {%- if page.content != blank -%}
    <div class="page-content__body rte">{{ page.content }}</div>
  {%- endif -%}
</div>

{% schema %}
{
  "name": "Page content"
}
{% endschema %}
